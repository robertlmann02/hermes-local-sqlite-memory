name: mann_memory
version: 0.1.4
description: "Mann_Memory — private SQLite/FTS5 memory provider with review workflow, Memory Guard quarantine, namespaces, local graph context, and deterministic consolidation."
hooks:
  - sync_turn
  - on_session_end
  - on_memory_write
