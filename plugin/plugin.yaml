name: mann_memory
version: 0.1.8
description: "Mann_Memory — private SQLite/FTS5 memory provider with review workflow, Memory Guard quarantine, namespaces, local graph context, memory inspection/control, portability backup/export/import, and deterministic consolidation, conservative cleanup hygiene."
hooks:
  - sync_turn
  - on_session_end
  - on_memory_write
